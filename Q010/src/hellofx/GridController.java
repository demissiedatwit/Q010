// GridController.java
package hellofx;

import javafx.fxml.FXML;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextField;
import javafx.scene.layout.GridPane;

public class GridController {
    @FXML
    private GridPane grid; // Ensure this matches the fx:id in the FXML

    @FXML
    private TextField usernameField; // Ensure this matches the fx:id in the FXML

    @FXML
    private PasswordField passwordField; // Ensure this matches the fx:id in the FXML

    // Add any methods needed for interaction here
}
